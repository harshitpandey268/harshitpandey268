#include<iostream>
using namespace std;

int main()
{
    int area, base, height;

    cout<<"Enter base and height of triangle";
    cin>>base>>height;
    area = (base*height)/2;
    cout<<"Area of triangle is :-  "<<area;
    return 0;

}